import Ember from 'ember';

export default Ember.Component.extend({
    /**
     * Classes used by component
     * @var string
     */
    classNames: 'responsive-table striped shipment-history',
    /**
     * Tag Name for component
     * @var string
     */
    tagName: 'table'
});
